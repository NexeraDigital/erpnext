

Open question for review: For already-paid card receipts (step 6), do we want to post as a direct Journal Entry (simplest path) or as a Purchase Invoice against a "Credit Card Clearing" account (more documents, but the vendor shows up in vendor aging and spend-by-supplier reports)?
Direct JE is simpler to build; PI + Clearing gives the owner visibility into "how much do we spend at Amazon?" / "who are our biggest vendors?" out-of-the-box. We need to pick one before development starts. Current draft below assumes direct JE — flag if you want PI + Clearing instead.
Below is a walkthrough of the 13-step AP workflow, from invoice receipt through full reconciliation in ERPNext. The flow is designed around industry-standard controls: deduplication, per-field confidence scoring, gated supplier creation, doctype branching, amount-anomaly detection, segregation of duties, external bank-feed closure, and idempotent async processing throughout.
1. Receipt / Invoice Intake The process begins when an invoice or receipt arrives — emailed PDF, scanned paper document, phone-camera photo, or vendor portal pull. The document is logged into ERPNext immediately and attached to a new AP record so the artifact is timestamped and retrievable from the moment it enters the organization. No structured data is required from the vendor at this stage; the system accepts whatever format arrives.
2. Pre-Extraction Deduplication Before any extraction runs, the document is checked against the last 90 days of intake for duplicates. Two checks run in parallel: an exact file-hash check (catches re-uploads of the same image) and a fuzzy near-duplicate check (catches re-scans of the same receipt). Any hit short-circuits the workflow and surfaces the original record to the operator rather than starting a second extraction. This is the firewall against the most common AP error — paying or booking the same invoice twice.
3. AI Extraction with Per-Field Confidence A narrow AI layer reads the document and extracts structured fields: vendor name, invoice or receipt number, date, line items, subtotal, tax, total, and any visible PO or account reference. The extractor returns a confidence score per field, not just per document — extraction systems routinely produce a confident total while hallucinating the vendor, so a single document-level score is not safe to route on. The AI is scoped to extraction only; humans remain in control of validation and decisioning.
4. Supplier Resolution The extracted vendor string is resolved to an ERPNext Supplier record through a three-tier match. First, a deterministic alias table catches known variants (e.g., "AMZN Mktp US*4Z9" → "Amazon"). Second, fuzzy matching against existing suppliers handles new variants of known vendors. Third, if no match is found and confidence is high enough, the system queues a "create new supplier" request for reviewer approval. Auto-creating suppliers without a gate is a known data-quality hazard and a fraud vector.
5. GL Coding, Cost Center & Tax Assignment The expense account, cost center, and tax treatment are applied. ERPNext's Supplier doctype natively carries a per-Company default payable account via its Party Account child table; default cost center and default expense account are added through Custom Fields or a supplier-to-coding mapping table to drive auto-coding for routine vendors. For organizations with multiple locations or departments, the cost center can be inferred from the location on the receipt, the card last-4 used to pay, or the supplier's assigned cost center. Tax fields are populated from the extracted tax line and validated against the supplier's tax profile. Without coding at this step, nothing can auto-post and every document falls to manual review.
6. Document-Type Classification & Doctype Selection The document is classified and routed to the appropriate ERPNext doctype based on the nature of the transaction:
Unpaid vendor bill → Purchase Invoice (later paid via Payment Entry)
Already-paid card or cash receipt → Journal Entry (DR expense, CR card/cash liability)
Employee out-of-pocket expense → Expense Claim (later paid via Payment Entry)
Other → routed to review for manual classification
Routing everything to a single doctype causes double-counting of liabilities and breaks trial-balance reconciliation. This branch is the heart of correct ERPNext AP automation.
7. Validation, Anomaly Detection & Three-Way Match (where applicable) The system runs the full validation suite: required fields present, totals consistent with line items, vendor active, expense account valid, duplicate check confirmed, vendor bank details unchanged since last payment. An amount-anomaly check flags any invoice where the total is significantly out of line with this supplier's recent history (e.g., > 3x the 6-month rolling average), catching typo'd amounts, double-billing, and fraudulent invoices that a small AP team would otherwise miss. For unpaid vendor bills with a referenced PO, a three-way match is run against the Purchase Order and Goods Receipt — invoices that don't match the PO within tolerance are flagged. PO-less / two-way matching is acceptable for small environments but is an explicit policy choice, not a default.
8. Confidence-Based Routing (Auto-Post vs. Review Queue) Documents fork based on a combined signal of per-field confidence and validation flags. Items with all fields above the confidence threshold and no validation flags are auto-submitted as a draft to the appropriate doctype, queued for approval if over threshold. Items with any field below threshold or any validation flag raised are routed to the AP review queue with the exception reason surfaced (low vendor confidence, total mismatch, duplicate suspect, unrecognized supplier, etc.). This routing is what makes the workflow scalable.
9. AP Review (Exception Handling) For items in the review queue, an AP clerk addresses the surfaced exception: correcting a misread field, resolving a supplier ambiguity, completing GL coding, or rejecting the document back to the vendor with a reason. Rejected items are not dead-ends — they re-enter the workflow when corrected, and the rejection trail is preserved for audit. This step is also where vendor-pushback conversations live.
10. Approval Routing with Segregation of Duties Approved items are routed to the appropriate approver(s) based on policy — typically driven by amount thresholds, department, cost center, or supplier risk profile. Segregation of duties is enforced: the role that extracted/coded the document cannot also approve it above a defined threshold, and vendor master changes (especially bank-detail changes) follow a separate approval path from invoice approvals. This is the formal authorization gate.
11. Payment Execution For unpaid bills, the approved Purchase Invoice triggers a Payment Entry — either via the configured payment rail (ACH, check, card) or a controlled mock execution in pilot environments. For already-paid card receipts, this step is a no-op because payment already happened; the system advances directly to step 12. For employee reimbursements, the Expense Claim is paid out via Payment Entry on the configured cadence.
12. Bank-Feed Match & Reconciliation (mocked during development) The actual money movement is reconciled against the bank feed: card transactions match Journal Entries for already-paid receipts; ACH/check disbursements match Payment Entries for vendor bills and reimbursements. Exact-amount + date-window matching is used as the primary signal; fuzzy auto-matching by party name is treated as a hint, not a clearance. Bank-feed match is the external signal that closes the loop — until it lands, the transaction is not truly closed.
During development this step is mocked using fixture bank transactions that mirror the shape of real feed data. The matching logic, exception paths, and reconciliation reports are all exercised end-to-end against the mock; only the live feed connection is deferred until pre-cutover, at which point the mock is swapped for the real bank-feed integration with no other workflow changes.
13. Closure & Audit Trail The invoice, payment, GL entries, and bank-transaction match are linked and the transaction is closed. Trial-balance reconciliation runs against the prior period to detect any drift. Frappe's built-in Version doctype captures field-level history across the entire lifecycle, and attachments are retained for 7 years per IRS requirements. From this point the full audit chain — image, extraction, validation, approval, payment, bank-match, posting — is retrievable from a single record.
The Result: A traceable, auditable, fraud-resistant, end-to-end AP lifecycle inside ERPNext.
Core Inputs: Invoice/receipt image • Supplier master with per-Company defaults • Chart of accounts & cost centers • PO and goods-receipt records (where applicable) • Bank transaction feed
Control Points: Deduplication • Gated supplier creation • Per-field confidence thresholds • Three-way match (where PO exists) • Vendor bank-detail change workflow • Segregation of duties on approval • Bank-feed reconciliation
Final Outcomes: Correctly classified posting to the right ERPNext doctype • Payment confirmed by external bank feed • Trial balance reconciled • Full field-level audit history retained 7 years
Engineering note: Extraction and bank-feed sync run asynchronously through Frappe's background queue (frappe.enqueue) so the UI never blocks on AI calls. All downstream posting operations (Supplier creation, Purchase Invoice insert, Payment Entry, Journal Entry) use idempotency keys so a retry after a transient failure can't double-post and break the trial-balance reconciliation.
Business Value: Reduced manual data entry through OCR + auto-coding for routine vendors; faster AP cycle times via confidence-based straight-through processing; lower error rate from deduplication, validation, anomaly detection, and three-way matching; stronger fraud controls via SoD, gated supplier creation, and vendor-master change approval; audit-ready by construction with field-level version history.